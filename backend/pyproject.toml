[project]
name = "mittnaturkart"
version = "0.4.1"
description = "Nature observation tracking application"
requires-python = ">=3.12"

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"
